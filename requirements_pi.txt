# Python dependencies for Raspberry Pi client
# Install with: pip3 install -r requirements_pi.txt

# Core dependencies (required)
requests>=2.31.0
pycryptodome>=3.19.0
paho-mqtt>=2.0.0

# Sensor hardware libraries (install only what you need)
# For analog sensors (pH, TDS, Turbidity) with ADS1115:
adafruit-circuitpython-ads1x15>=2.2.15
adafruit-blinka>=8.20.0

# For analog sensors with MCP3008 (alternative to ADS1115):
# adafruit-circuitpython-mcp3xxx>=2.4.0

# For DS18B20 temperature sensor:
w1thermsensor>=2.0.0

# For DHT22/DHT11 temperature and humidity:
adafruit-circuitpython-dht>=2.4.0

# For GPIO access (if needed):
RPi.GPIO>=0.7.1

# For USB Serial sensors (VirtualBox compatible):
pyserial>=3.5

